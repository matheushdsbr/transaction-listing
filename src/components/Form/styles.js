import styled from 'styled-components';

export const FormField = styled.form`
  position: relative;
  width: 100%;

  img {
    position: absolute;
    top: 50%;
    right: 25px;
    transform: translateY(-50%);
  }
`;
